<UserControl x:Class="RemiFetchFrame.Views.Extract_From_iTunes.ExtractFromiTunesWizard1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RemiFetchFrame.Views.Extract_From_iTunes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20" Background="#fafafa">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Wizard Header -->
        <Border Grid.Row="0" Background="#f5f5f5" BorderBrush="#d1d5db" BorderThickness="0,0,0,1" Padding="20,8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Margin="0,0,10,0">
                    <Border Background="#1565c0" CornerRadius="100" Width="28" Height="28">
                        <TextBlock Text="1" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontWeight="SemiBold"/>
                    </Border>
                    <TextBlock Text=" Select iTunes Backup " Margin="8,0,0,0" Foreground="#1565c0" FontSize="13" FontWeight="SemiBold"/>
                </StackPanel>
                <Rectangle Fill="#d1d5db" Width="40" Height="1" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" Margin="10,0,10,0">
                    <Border Background="#e5e7eb" CornerRadius="100" Width="28" Height="28">
                        <TextBlock Text="2" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#374151" FontWeight="SemiBold"/>
                    </Border>
                    <TextBlock Text=" Choose Data Types " Margin="8,0,0,0" Foreground="#4b5563" FontSize="13"/>
                </StackPanel>
                <Rectangle Fill="#d1d5db" Width="40" Height="1" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal">
                    <Border Background="#e5e7eb" CornerRadius="100" Width="28" Height="28">
                        <TextBlock Text="3" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#374151" FontWeight="SemiBold"/>
                    </Border>
                    <TextBlock Text=" Extract &amp; Export " Margin="8,0,0,0" Foreground="#4b5563" FontSize="13"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Main Body -->
        <StackPanel Grid.Row="1" Margin="10" Background="White" Orientation="Vertical">
            <TextBlock Text="Step 1: Select iTunes Backup Source" FontSize="16" FontWeight="SemiBold" Foreground="#1f2937" Margin="0,0,0,10"/>

            <!-- Folder Selection Controls -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <Button x:Name="BtnAddFolder" Content="Add Folder..." Width="140" Height="32" Background="#1565c0" Foreground="White" BorderThickness="0" Margin="0,0,10,0" /> <!--Click="OnAddFolder"-->
                <Button x:Name="BtnRemoveSelected" Content="Remove Selected" Width="150" Height="32" Background="#e5e7eb" Foreground="#111827" BorderThickness="0" Margin="0,0,10,0" Click="OnRemoveSelected"/>
                <Button x:Name="BtnClearList" Content="Clear List" Width="120" Height="32" Background="#e5e7eb" Foreground="#111827" BorderThickness="0" Click="OnClearList"/>
            </StackPanel>

            <!-- Preview Table -->
            <Border BorderBrush="#d1d5db" BorderThickness="1" CornerRadius="6" Padding="12" Background="#f9fafb">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Imported iTunes Backups" FontWeight="SemiBold" FontSize="14" Foreground="#1f2937" Margin="0,0,0,8"/>

                    <DataGrid x:Name="BackupList"
                              Grid.Row="1"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              HeadersVisibility="Column"
                              Background="White"
                              BorderThickness="0"
                              AlternatingRowBackground="#f9fafb"
                              FontSize="12">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Backup Name" Binding="{Binding Name}" Width="*"/>
                            <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="2*"/>
                            <DataGridTextColumn Header="Size" Binding="{Binding Size}" Width="*"/>
                            <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Footer -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button x:Name="BtnNext" Content="Next →" Width="120" Height="32" Background="#1565c0" Foreground="White" BorderThickness="0" /> <!--Click="OnNext"-->
            </StackPanel>
        </StackPanel>
    </Grid>

</UserControl>
